# next_word_prediction
This is a Next Word Prediction Model built using deep learning.
The model takes a seed text of any length from the user and predicts a number of words specified by the user.
The model restricts the user to input only valid integers as the number of words to predict. And takes 0 to break.
